export interface Book {
    id: string;
    title: string;
    author: string;
    quantity: number;
  }
  
  export interface BookInput {
    title: string;
    author: string;
    quantity: number;
  }
  